/**
 * String utilities for mission text handling
 */

/**
 * Creates a properly formatted mission text string that can handle apostrophes and quotes
 * @param {String} text - The text content
 * @returns {String} A properly formatted string
 */
export function missionText(text) {
  return text;
}

// Export a way to safely create text with apostrophes
export const t = missionText;